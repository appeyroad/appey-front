git config --global user.name "name"
git config --global user.email "email"

mkdir ~/MyProject 
cd ~/myproject 
git init        
git status       
git add .          
git commit -m "commit name"

git remote add origin https://github.com/username/myproject.git
git remote set-url origin https://github.com/HFTshoon/public.git
git remote set-url origin https://github.com/appeyroad/s14-todo.git

git remote rm myJSDev
git remote -v 
git push origin master

git pull
git add .
git commit -m 'First commit'
git remote add origin https://github.com/appeyroad/s14-todo.git
git checkout -b "name"
git branch -D master
git push -u origin "name"